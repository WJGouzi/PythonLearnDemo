面向对象的中高级

class Foo:
    def __init__(self, name):
        # 普通字段
        self.name = name

    # 普通方法
    def bar(self):
        pass

obj = Foo('wj')
obj.name
obj.bar()


类成员:
    字段
        普通字段-> 保存在对象里
        静态字段-> 保存在类里

    方法-> 保存在类类



class Province:

    # 静态字段
    country = '中国'

    def __init__(self, name):
        # 普通字段
        self.name = name

Province.country    #  可以直接访问
sichuan = Province('四川')
name = sichuan.name
